# Components
This folder contains reusable React components.

## Component Organization:
- `/common/` - Shared components (Header, Footer, Navigation, etc.)
- `/ui/` - UI components (Button, Card, Form elements, etc.)
- `/forms/` - Form components (ContactForm, LoginForm, etc.)
- `/sections/` - Page sections (HeroSection, FeaturedProjects, etc.)
